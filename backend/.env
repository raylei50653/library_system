DJANGO_SECRET_KEY='h(b*k+t_^%dgjq)%dg5@+d66_hee4_ul+q=&dr!lf5gs_d!b(l'
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# 宿主工具使用；容器內會透過 compose 環境覆蓋為 db:5432
DATABASE_URL=postgresql://ray:ray123456@127.0.0.1:5432/library_db

CORS_ALLOWED_ORIGINS=http://localhost:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173

JWT_ACCESS_MIN=90
JWT_REFRESH_DAYS=14

LOG_LEVEL=DEBUG

# --- AI / Chat ---
CHAT_AI_ENABLED=true
CHAT_AI_PROVIDER=ollama
CHAT_AI_MODEL=llama3.1:8b-instruct
OLLAMA_BASE_URL=http://ollama:11434   # 鍵名改成大多數程式碼會讀的這個
OLLAMA_TIMEOUT_SEC=120                # 首輪預熱常>30s，先拉高

# --- Postgres 初始化（供 db 服務使用） ---
POSTGRES_DB=library_db
POSTGRES_USER=USER
POSTGRES_PASSWORD=PASSWORD
